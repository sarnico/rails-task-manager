<h1>ca marche ???</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to "#{task.title}", task_path(task) %> <%= link_to "edit", edit_path(task)  %>  <%= link_to "delete", destroy_path(task), method: :delete %>
  </li>
  <% end %>
</ul>
<%= link_to "create a new task", new_path  %>


